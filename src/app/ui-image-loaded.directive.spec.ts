import { UiImageLoadedDirective } from './ui-image-loaded.directive';

describe('UiImageLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new UiImageLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
