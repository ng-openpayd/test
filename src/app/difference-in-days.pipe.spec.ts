import { DifferenceInDaysPipe } from './difference-in-days.pipe';

describe('DifferenceInDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new DifferenceInDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
